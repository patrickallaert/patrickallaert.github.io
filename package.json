{
  "name": "conexao-be",
  "private": true,
  "version": "0.1.0",
  "description": "Static site rebuild for Conexao",
  "scripts": {
    "build:html": "node scripts/build-classes.js && node scripts/build-levels.js && node scripts/build-venues.js && node scripts/build-registration.js && node scripts/build-featured-event.js",
    "build:css": "sass --no-source-map src/styles/site.scss docs/assets/site.css",
    "build": "npm run build:html && npm run build:css",
    "lint:css": "stylelint \"src/styles/**/*.scss\"",
    "test:contrast": "node scripts/check-contrast.js",
    "verify": "npm run lint:css && npm run build && npm run test:contrast",
    "watch:css": "sass --watch src/styles/site.scss:docs/assets/site.css"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "postcss-scss": "^4.0.9",
    "sass": "^1.93.2",
    "stylelint": "^17.6.0",
    "stylelint-config-recess-order": "^7.7.0"
  }
}
