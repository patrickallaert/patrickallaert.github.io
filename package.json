{
  "name": "conexao-be",
  "private": true,
  "version": "0.1.0",
  "description": "Static site rebuild for Conexao",
  "scripts": {
    "build:css": "sass --no-source-map src/styles/site.scss site/assets/site.css",
    "build": "npm run build:css",
    "watch:css": "sass --watch src/styles/site.scss:site/assets/site.css"
  },
  "devDependencies": {
    "sass": "^1.93.2"
  }
}
